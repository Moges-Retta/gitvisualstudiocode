const prompt = require("prompt-sync")();

/*function even(getal){
    remainder=(getal%2);
    if (remainder==0){isEvenNummer='True'}
    else{isEvenNummer='False'}
    return isEvenNummer;
}
const getal=Number(prompt('enter getal'))
aantword=even(getal)
console.log('is nummer even?',aantword)*/
"use strict";
const voornamen = [ "Aeneas", "Alissia", "Anaïs" ];
for (const voornaam of voornamen) { 
console.log(voornaam);
}